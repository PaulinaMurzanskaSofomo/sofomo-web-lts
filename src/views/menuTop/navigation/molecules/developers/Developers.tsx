import React, { FC } from "react";

interface Props {
  id?: any;
  active?: boolean;
}

const Developers: FC<Props> = ({ id, active }) => {
  return <div>Developers</div>;
};

export default Developers;
